<h2>Welcome <%= current_user.username %></h2>
<hr />
<%= link_to "Add Playlist", new_playlist_path %>
<h3>Your Playlists</h3>
<ul>
  <% @playlists.each do |playlist| %>
    <li>
      <%= playlist.name %> 
      <%= link_to 'View Playlist', playlist_path(playlist) %>
    </li>
  <% end %>
</ul>